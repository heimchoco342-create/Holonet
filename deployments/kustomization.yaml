apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

namespace: holonet

resources:
  - namespace.yaml
  # postgres-secret.yaml should be created manually or from postgres-secret.yaml.example
  # - postgres-secret.yaml
  - postgres-pvc.yaml
  - postgres-deployment.yaml
  - server-configmap.yaml
  - server-deployment.yaml

# 이미지 태그 오버라이드 (예: production, staging)
# images:
#   - name: holonet/server
#     newTag: v1.0.0

# 리소스 오버라이드 (환경별)
# replicas:
#   - name: holonet-server
#     count: 3
