version: '3.8'

# 기존 PostgreSQL 사용 시 아래 서비스는 사용하지 않습니다
# 포트포워딩으로 localhost:5432에 연결하세요
# services:
#   postgres:
#     image: postgres:16-alpine
#     container_name: holonet-postgres
#     environment:
#       POSTGRES_USER: holonet
#       POSTGRES_PASSWORD: holonet
#       POSTGRES_DB: holonet
#     ports:
#       - "5432:5432"
#     volumes:
#       - postgres_data:/var/lib/postgresql/data
#     healthcheck:
#       test: ["CMD-SHELL", "pg_isready -U holonet"]
#       interval: 10s
#       timeout: 5s
#       retries: 5

# volumes:
#   postgres_data:
